language: java
jdk: oraclejdk8
sudo: false
cache:
  directories:
  - "$HOME/.m2"
  - "$HOME/.sonar/cache"
addons:
  sonarqube: true
script: mvn -DskipITs=true clean org.jacoco:jacoco-maven-plugin:prepare-agent install
after_success: mvn sonar:sonar